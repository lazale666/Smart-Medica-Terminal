# 快速开始

本文档面向第一次运行本项目的使用者，按“先服务端、再客户端”的顺序完成。

## 1. 准备环境

请先确认以下软件已经安装：

- Python 3.8 及以上
- Qt 6.x
- MinGW 64-bit
- Ollama

普通用户客户端额外需要 Qt 模块：

- `Qt Widgets`
- `Qt Network`
- `Qt Multimedia`
- `Qt TextToSpeech`

医生客户端需要：

- `Qt Widgets`
- `Qt Network`
- `Qt Multimedia`

## 2. 启动服务端

进入服务端目录：

```powershell
cd .\Server_python
```

安装依赖：

```powershell
pip install -r requirements.txt
```

准备 Ollama 模型：

```powershell
ollama pull qwen2.5:7b
```

启动服务端：

```powershell
python server.py
```

如果你在 Windows 上，也可以直接双击：

```text
Server_python/启动服务器.bat
```

正常启动后，会看到类似日志：

```text
AI Chat Server Starting...
[OK] Database initialized
[OK] AI model 'qwen2.5:7b' loaded successfully
[OK] Server listening on: 0.0.0.0:9999
Waiting for client connections...
```

默认监听地址：

- IP：`0.0.0.0`
- 端口：`9999`

## 3. 构建普通用户客户端

```powershell
cd .\Client_Qt\Client
qmake dontknow.pro
mingw32-make
```

常见输出位置：

- 命令行原地构建：`Client_Qt/Client/debug/` 或 `Client_Qt/Client/release/`
- Qt Creator 阴影构建：`build/...`

## 4. 构建医生客户端

```powershell
cd .\Client_Qt\Client_Doctor
qmake medical.pro
mingw32-make
```

常见输出位置：

- 命令行原地构建：`Client_Qt/Client_Doctor/debug/` 或 `Client_Qt/Client_Doctor/release/`
- Qt Creator 阴影构建：`build/...`

## 5. 首次使用流程

### 普通用户端

1. 启动普通用户客户端。
2. 在登录页注册本地账号。
3. 登录后进入主菜单。
4. 默认服务器地址使用 `127.0.0.1:9999` 即可。
5. 点击“问诊咨询”即可与 AI 对话。

说明：

- 普通用户账号默认保存在客户端目录下的 `users.json`
- 非会员每天最多免费问诊 `5` 次
- “病历记录”和“名医对话”默认需要会员权限

### 医生端

1. 启动医生客户端。
2. 在登录页注册本地医生账号。
3. 登录后客户端会连接服务端，并使用 `doctor` 角色登录。
4. 等待患者连接。

说明：

- 医生本地账号保存在医生客户端目录下的 `doctors.json`
- 聊天历史保存在应用目录下 `chat_history_doctor/<医生名>/`

## 6. 语音相关

普通用户端支持：

- 按住录音按钮录音
- 调用百度语音识别将录音转文字
- 使用 `QTextToSpeech` 朗读 AI 回复

如果语音输入不可用，请先阅读 [配置说明_BAIDU_VOICE.md](配置说明_BAIDU_VOICE.md)。

## 7. 常见问题

### 服务端启动失败

- 检查 `python --version`
- 检查 `pip install -r requirements.txt` 是否成功
- 检查 `ollama --version`
- 检查 `ollama pull qwen2.5:7b` 是否已完成
- 检查端口 `9999` 是否被占用

### Qt 编译失败

- 检查 Qt 版本是否包含所需模块
- 检查 `qmake` 和 `mingw32-make` 是否在 `PATH`
- 确认普通用户端使用的是 `dontknow.pro`
- 确认医生端使用的是 `medical.pro`

### 客户端连不上服务端

- 检查服务端是否正在运行
- 检查客户端配置的 IP / 端口是否正确
- 检查 Windows 防火墙是否放行 `9999`

### 医生端看不到患者病历

这是当前实现的已知限制。医生端读取的是本机用户目录下的病历文件，不是服务端集中存储。

## 8. 下一步

建议继续阅读：

- [技术文档.md](技术文档.md)
- [Client_Qt/构建说明.md](Client_Qt/构建说明.md)
