# 快速开始指南

## 3 分钟快速上手

### 第一步：启动服务器

1. 打开命令行/终端
2. 进入服务器目录：
   ```bash
   cd Project_End/Server
   ```
3. 运行服务器：
   ```bash
   python server.py
   ```
   （Windows 用户可直接双击 `启动服务器.bat`
4. 等待服务器启动，你会看到以下输出：
   ```
   =============================================================
   AI Chat Server Starting...
   =============================================================
   [OK] Ollama service is running
   Loading AI model: qwen2.5:7b...
   [OK] AI model 'qwen2.5:7b' loaded successfully
   [OK] Server listening on: 0.0.0.0:9999
   =============================================================
   Waiting for client connections...
   =============================================================
   ```

### 第二步：编译和运行客户端

#### 方法一：使用 Qt Creator（推荐）

1. 打开 Qt Creator
2. 打开 `Project_End/Client_Qt/Client/dontknow.pro`
3. 点击"配置项目"
4. 点击左下角的"运行"按钮编译并运行

#### 方法二：命令行编译

```bash
cd Project_End/Client_Qt/Client
qmake dontknow.pro
mingw32-make
```

### 第三步：使用客户端

#### 登录/注册

1. 打开客户端程序
2. 在登录界面输入任意用户名和密码
3. 点击"注册"（首次使用请先注册
4. 或点击"登录"进入主界面

#### 连接服务器

1. 在服务器地址栏输入：`127.0.0.1`
2. 在端口栏输入：`9999`
3. 点击"连接服务器"
4. 等待连接成功提示

#### 开始对话

1. 在输入框中输入消息
2. 点击"发送"按钮
3. 等待 AI（茯苓）回复

#### 语音输入

1. 按住麦克风按钮开始录音
2. 松开按钮结束录音

## 修复的关键问题说明

### 问题：发送消息服务器收不到

**原因**：
1. 没有发送 4 字节长度头
2. 字段名不匹配

**已修复**：
- ✅ 客户端发送消息时先发送 4 字节大端序长度
- ✅ 字段名统一使用 `data`
- ✅ 服务器兼容旧版本的 `message` 字段

## 下一步

- 阅读完整的 README.md 了解更多功能
- 阅读配置说明配置语音功能
- 根据需要修改代码添加更多功能

祝使用愉快！
