# 技术文档 - 茯苓AI医疗智能助手

## 1. 项目概述

### 1.1 项目简介
**茯苓** 是一个基于Qt框架的智能医疗助手客户端，配合Python后端服务器和Ollama AI模型，实现智能问答功能。

### 1.2 技术栈
- **客户端**：Qt 6.11.1 + C++17
- **服务器**：Python 3.x + Flask/Tornado
- **AI模型**：Ollama (qwen2.5:7b)
- **语音识别**：百度智能云语音识别API
- **通信协议**：TCP + JSON

---

## 2. 系统架构

### 2.1 整体架构
```
┌─────────────────┐
│   Qt客户端      │
│  (widget.cpp)   │
├─────────────────┤
│  网络通信层      │
│  (TCP Socket)   │
└────────┬────────┘
         │
    TCP/IP协议
         │
┌────────▼────────┐
│  Python服务器   │
│  (server.py)   │
├─────────────────┤
│  Ollama AI      │
│  (qwen2.5:7b)  │
└─────────────────┘
```

### 2.2 客户端架构
```
┌────────────────────────────────┐
│      UI层 (widget.ui)          │
│  ├─ 聊天显示 (textBrowser)    │
│  ├─ 消息输入 (lineEdit)       │
│  ├─ 语音按钮 (voiceBtn)       │
│  ├─ 进度条 (historyProgressBar)│
│  └─ 连接设置 (IP/Port)        │
└──────────────┬─────────────────┘
               │
┌──────────────▼─────────────────┐
│      业务逻辑层 (widget.cpp)    │
│  ├─ 网络管理 (NetworkManager)  │
│  ├─ 语音识别 (Speech)          │
│  ├─ 音频录制 (Audio)           │
│  ├─ 对话历史 (ChatHistory)     │
│  └─ 思考/中断控制              │
└──────────────┬─────────────────┘
               │
┌──────────────▼─────────────────┐
│      数据层                     │
│  ├─ JSON序列化/反序列化        │
│  ├─ 文件I/O (chat_history)     │
│  └─ 百度API调用 (HTTP)         │
└────────────────────────────────┘
```

### 2.3 服务器架构
```
┌────────────────────────────────┐
│      服务器主程序 (server.py)  │
├────────────────────────────────┤
│  网络层                         │
│  ├─ TCP服务器 (端口9999)       │
│  ├─ 多客户端支持               │
│  └─ 粘包处理                   │
├────────────────────────────────┤
│  业务逻辑层                     │
│  ├─ 消息解析                   │
│  ├─ Ollama集成                 │
│  └─ AI响应处理                 │
├────────────────────────────────┤
│  调试输出层                     │
│  ├─ 模型状态                   │
│  ├─ 连接状态                   │
│  └─ 消息收发日志               │
└────────────────────────────────┘
```

---

## 3. 通信协议

### 3.1 协议格式
采用 **长度前缀 + JSON数据** 格式

```
┌─────────────┬──────────────────────┐
│  4字节头    │    JSON数据          │
│  (大端序)   │   (UTF-8编码)        │
└─────────────┴──────────────────────┘
```

### 3.2 消息结构

#### 客户端发送消息
```json
{
  "type": "message",
  "data": "用户输入的文本"
}
```

#### 服务器响应消息
```json
{
  "type": "ai_response",
  "data": "AI生成的响应文本"
}
```

### 3.3 字段说明
| 字段 | 类型 | 说明 |
|------|------|------|
| type | string | 消息类型：message（用户消息）、ai_response（AI响应） |
| data | string | 消息内容 |

---

## 4. 核心功能实现

### 4.1 网络通信

#### 客户端发送消息流程
1. 构建JSON对象
2. 序列化为UTF-8字节流
3. 计算字节长度
4. 转换为4字节大端序长度头
5. 拼接：长度头 + JSON数据
6. 通过TCP Socket发送

```cpp
// widget.cpp - 发送消息
QByteArray jsonData = QJsonDocument(obj).toJson(QJsonDocument::Compact);
QByteArray packet;
QDataStream stream(&packet, QIODevice::WriteOnly);
stream.setByteOrder(QDataStream::BigEndian);
stream << (quint32)jsonData.size();
packet.append(jsonData);
socket->write(packet);
socket->flush();
```

#### 客户端接收消息流程
1. 监听 `readyRead()` 信号
2. 读取所有可用数据到缓冲区
3. 检查缓冲区大小（至少4字节）
4. 读取4字节长度头，转换为整数
5. 检查是否收到完整数据
6. 解析JSON数据
7. 处理业务逻辑
8. 移除已处理数据

```cpp
// widget.cpp - 接收消息
buffer.append(socket->readAll());
while (true) {
    if (buffer.size() < 4) break;
    quint32 dataLen = qFromBigEndian<quint32>(reinterpret_cast<uchar*>(buffer.data()));
    if (buffer.size() < 4 + dataLen) break;
    
    QByteArray jsonData = buffer.mid(4, dataLen);
    buffer = buffer.mid(4 + dataLen);
    
    QJsonDocument doc = QJsonDocument::fromJson(jsonData);
    // 处理JSON...
}
```

### 4.2 语音识别

#### 集成百度语音API
- 录音格式：PCM, 16000Hz, 16bit, 单声道
- API端点：`http://vop.baidu.com/server_api`
- 认证方式：OAuth 2.0

#### 语音识别流程
1. 用户按住麦克风按钮开始录音
2. 松开按钮停止录音
3. 读取录音文件
4. 获取百度Access Token
5. 调用语音识别API
6. 解析返回结果
7. 将识别文本发送到服务器

### 4.3 聊天历史

#### 存储格式
文件路径：`程序目录/chat_history/chat.txt`

```
时间戳|角色|消息内容
2024-05-27 14:30:00|我|你好
2024-05-27 14:30:02|茯苓|你好！有什么可以帮助你的吗？
```

#### 功能实现
- **自动保存**：每条消息实时保存
- **自动加载**：连接服务器后显示加载进度条
- **历史回显**：支持查看历史对话

### 4.4 思考与中断

#### 状态管理
```cpp
bool m_isThinking;      // AI是否正在思考
bool m_isInterrupted;    // 用户是否点击中断
QString m_pendingUserMessage;  // 待处理的用户消息
```

#### 工作流程
1. 用户发送消息
2. 显示"思考中..."，按钮变为"中断"
3. AI生成响应
4. 如果用户已中断：显示"已中断"，不记录
5. 如果正常：显示AI响应，记录对话

---

## 5. 文件结构

### 5.1 客户端文件
```
Client_Qt/Client/
├── main.cpp              # 程序入口
├── widget.h/cpp          # 主窗口类
├── widget.ui             # UI界面文件
├── audio.h/cpp           # 音频录制
├── speech.h/cpp          # 语音识别
├── http.h/cpp            # HTTP请求
├── dialog.h/cpp/ui       # 重连对话框
├── loginwidget.h/cpp/ui  # 登录界面
├── mainwindow.h/cpp/ui   # 主窗口界面
└── dontknow.pro         # Qt项目文件
```

### 5.2 服务器文件
```
Server/
├── server.py             # 服务器主程序
├── requirements.txt      # Python依赖
└── 启动服务器.bat        # 启动脚本
```

---

## 6. 依赖说明

### 6.1 Qt模块
- `QtWidgets` - GUI组件
- `QtNetwork` - 网络通信
- `QtMultimedia` - 音频录制
- `QtCore` - 核心功能

### 6.2 Python依赖
- `ollama` - Ollama AI模型接口
- `socket` - 网络通信（内置）
- `json` - JSON序列化（内置）

### 6.3 外部API
- **百度智能云语音API** - 语音识别
  - 需要：API Key, Secret Key
  - 端点：`https://vop.baidu.com/server_api`

---

## 7. 配置说明

### 7.1 百度语音API配置
在 `speech.h` 中配置：
```cpp
const QString client_id = "你的API_KEY";
const QString client_secret = "你的SECRET_KEY";
```

### 7.2 服务器配置
在客户端UI中配置：
- IP地址：默认 127.0.0.1
- 端口号：默认 9999

### 7.3 Ollama配置
服务器端默认使用：
- 模型：`qwen2.5:7b`
- 自动检测并启动Ollama服务

---

## 8. 调试与日志

### 8.1 服务器调试输出
```
[INFO] Ollama服务状态: 运行中
[INFO] 模型加载状态: qwen2.5:7b 已就绪
[INFO] 客户端连接: 192.168.1.100:54321
[INFO] 收到消息: 长度=1024字节
[OK] AI响应已发送
```

### 8.2 客户端调试
- 使用Qt Creator的"应用程序输出"面板
- 查看网络请求/响应
- 检查JSON解析错误

---

## 9. 性能优化

### 9.1 网络优化
- 使用TCP长连接
- 避免频繁创建/销毁Socket
- 使用缓冲区处理粘包

### 9.2 UI优化
- 使用信号槽异步更新UI
- 避免在主线程执行耗时操作
- 使用进度条改善用户体验

### 9.3 存储优化
- 聊天记录按时间戳存储
- 支持文件轮转（可选）
- 使用QFile进行高效I/O

---

## 10. 安全考虑

### 10.1 敏感信息保护
- 百度API密钥硬编码风险 → 建议使用配置文件或环境变量
- 网络传输为明文 → 生产环境建议使用SSL/TLS

### 10.2 输入验证
- 空消息过滤
- JSON格式校验
- 网络异常处理

---

## 11. 扩展建议

### 11.1 功能扩展
- 支持图片上传/识别
- 支持文件传输
- 多语言支持
- 离线模式

### 11.2 架构优化
- 使用Qt Quick/QML重构UI
- 引入数据库存储聊天记录
- 添加用户认证系统
- 实现消息加密

### 11.3 性能提升
- 使用WebSocket替代TCP
- 实现消息压缩
- 添加缓存机制
- 负载均衡支持

---

## 12. 版本信息

- **客户端版本**：1.0.0
- **服务器版本**：1.0.0
- **Qt版本**：6.11.1
- **Python版本**：3.x
- **最后更新**：2024-05-27
