# 技术文档

本文档基于当前仓库源码整理，重点描述真实实现，而不是理想设计。

## 1. 总体架构

项目由三部分组成：

1. `Server_python/server.py`
   负责 TCP 接入、AI 调用、在线医生管理、消息转发。

2. `Client_Qt/Client`
   普通用户客户端，负责本地登录、AI 问诊、病历管理、医生咨询、会员与设置。

3. `Client_Qt/Client_Doctor`
   医生客户端，负责本地登录、医生侧会话管理、历史记录和病历查看。

## 2. 服务端设计

### 2.1 关键依赖

- `socket`
- `threading`
- `sqlite3`
- `langchain_ollama.ChatOllama`

### 2.2 关键全局状态

- `ollm`
  Ollama 模型实例。

- `clients`
  当前连接的 socket 列表。

- `doctors`
  在线医生列表，元素包含医生 socket、用户名、已连接患者等状态。

- `database_path`
  默认是 `smart_medica.db`。

### 2.3 数据库结构

服务端启动时会初始化以下表：

```sql
users(id, username, password, role)
sessions(id, client_id, doctor_id, start_time, end_time)
messages(id, session_id, sender, content, timestamp)
```

说明：

- `users` 表会被登录 / 自动注册逻辑使用。
- `sessions` 和 `messages` 目前已建表，但当前代码没有完整写入会话与消息。

### 2.4 Ollama 初始化

服务端启动时会：

1. 检查本机 `http://localhost:11434`
2. 如果未运行则尝试 `ollama serve`
3. 加载模型 `qwen2.5:7b`

当前模型名写死在：

```python
model_name = "qwen2.5:7b"
```

## 3. 通信协议

### 3.1 数据包格式

所有 TCP 消息格式统一为：

```text
[4 字节大端长度][UTF-8 JSON]
```

长度字段表示 JSON 字节数，不包含这 4 个字节自身。

### 3.2 主要请求类型

| 类型 | 发起方 | 说明 |
| --- | --- | --- |
| `login` | 医生端 / 其他客户端 | 服务端登录 |
| `register` | 预留 | 服务端注册 |
| `message` | 普通用户端 / 病历 AI 填充 | 发送 AI 问诊消息 |
| `ping` | 测试脚本 | 心跳 |
| `get_doctors` | 普通用户端 | 获取在线医生列表 |
| `connect_doctor` | 普通用户端 | 连接指定医生 |
| `request_doctor` | 预留客户端流程 | 自动分配医生 |
| `doctor_message` | 患者端 / 医生端 | 医患消息转发 |

### 3.3 主要响应类型

| 类型 | 说明 |
| --- | --- |
| `login_success` | 登录成功 |
| `login_failed` | 登录失败 |
| `register_success` | 注册成功 |
| `register_failed` | 注册失败 |
| `ai_response` | AI 回复 |
| `pong` | 心跳响应 |
| `doctor_list` | 在线医生列表 |
| `connection_success` | 医患连接成功 |
| `connection_failed` | 医患连接失败 |
| `waiting_for_doctor` | 当前没有在线医生 |
| `new_client` | 通知医生有新患者接入 |
| `client_message` | 转发后的医患消息 |
| `client_disconnected` | 患者断开 |
| `doctor_disconnected` | 医生断开 |

### 3.4 示例

普通 AI 问诊请求：

```json
{
  "type": "message",
  "data": "我最近头痛，应该怎么办？"
}
```

医生连接请求：

```json
{
  "type": "connect_doctor",
  "doctor_id": 12345678,
  "username": "alice"
}
```

医生回消息：

```json
{
  "type": "doctor_message",
  "message": "建议先测量体温并观察症状变化",
  "target_session_id": "1c4d0f2d-xxxx-xxxx"
}
```

## 4. 普通用户客户端

### 4.1 主体页面

- `LoginWidget`
  本地注册 / 登录，读写 `users.json`

- `MenuWidget`
  主菜单，展示连接状态、会员权限入口

- `Widget`
  AI 问诊聊天页

- `MedicalRecordWidget`
  病历管理页

- `DoctorListWidget`
  在线医生列表页

- `DoctorDialog`
  患者与医生的单会话窗口

- `MemberRechargeWidget`
  会员开通页

- `SettingsWidget`
  用户信息、主题、缓存、服务器、朗读设置

### 4.2 登录机制

普通用户端当前不是服务端登录，而是本地文件登录：

- 文件名：`users.json`
- 存放位置：客户端当前工作目录

这意味着：

- 更换机器后账号不会自动同步
- 服务端数据库中的 `users` 表并不是普通用户登录的唯一真源

### 4.3 AI 问诊

文件：

- `Client_Qt/Client/widget.cpp`

特点：

- 发送 `message`
- 服务端返回 `ai_response`
- 结果写入聊天区并保存到本地历史文件
- 非会员每天默认只能免费咨询 5 次

免费次数保存在 `QSettings`：

- `freeConsultDate_<username>`
- `freeConsultCount_<username>`

### 4.4 会员限制

当前逻辑：

- AI 问诊：非会员也可使用，但每天仅 5 次
- 病历记录：需要会员
- 名医对话：需要会员

会员状态通过 `QSettings` 保存，键名形如：

```text
member_<username>
```

### 4.5 病历记录

文件：

- `Client_Qt/Client/medicalrecordwidget.cpp`

病历文件保存路径：

```text
%USERPROFILE%/SmartMedica/records/<用户名>/record_yyyyMMdd_hhmmss.txt
```

病历文件是纯文本，内容格式类似：

```text
疾病名称:感冒
诊断日期:2026-05-29
治疗建议:多饮水，注意休息
```

### 4.6 聊天历史

普通用户 AI 聊天历史保存在：

```text
<应用目录>/chat_history/<用户名>/chat_*.txt
```

每行格式：

```text
时间|角色|消息内容
```

### 4.7 语音功能

分为两部分：

1. 录音转文字
   通过 `Audio` 录音，再由 `Speech::speechIdentify()` 调用百度语音识别。

2. 朗读 AI 回复
   使用 `QTextToSpeech`，不是百度 TTS。

## 5. 医生客户端

### 5.1 登录机制

医生端首先走本地登录：

- 文件名：`doctors.json`
- 存放位置：医生客户端当前工作目录

本地登录成功后，`DoctorChatWidget` 会向服务端发送：

```json
{
  "type": "login",
  "username": "...",
  "password": "...",
  "role": "doctor"
}
```

### 5.2 会话管理

文件：

- `Client_Qt/Client_Doctor/doctorchatwidget.cpp`

特点：

- 支持多患者会话列表
- 每个患者拥有独立 `session_id`
- 未读数与最后消息预览由医生端本地维护

### 5.3 医生历史记录

医生侧聊天历史保存在：

```text
<应用目录>/chat_history_doctor/<医生名>/doctor_chat_*.txt
```

每行格式：

```text
时间|角色|发送者|消息内容
```

### 5.4 病历查看机制

医生端查看病历并不是请求服务端，而是直接读取本机目录：

```text
%USERPROFILE%/SmartMedica/records/<患者名>/
```

这意味着：

- 只有在同机或共享同一用户目录时，医生才可能读到患者病历
- 当前不是分布式可用方案

## 6. 设置与配置

### 6.1 普通用户端 `QSettings`

组织名 / 应用名：

```text
SmartMedica / Client
```

主要存储：

- 服务器 IP / 端口
- 自动连接
- 模式
- 字体颜色
- 背景样式
- 朗读音量 / 语速
- 会员状态
- 免费咨询次数
- 用户性别 / 年龄

### 6.2 医生端 `QSettings`

组织名 / 应用名：

```text
SmartMedica / DoctorClient
```

主要存储：

- 服务器 IP / 端口
- 医生个人信息

## 7. 构建说明摘要

### 普通用户端

```powershell
cd .\Client_Qt\Client
qmake dontknow.pro
mingw32-make
```

依赖模块：

- `widgets`
- `network`
- `multimedia`
- `texttospeech`

### 医生端

```powershell
cd .\Client_Qt\Client_Doctor
qmake medical.pro
mingw32-make
```

依赖模块：

- `widgets`
- `network`
- `multimedia`

## 8. 当前限制与技术债

1. 普通用户账号、医生账号、服务端数据库账号三套体系并存。
2. 医生病历查看依赖本地文件共享，跨机不可用。
3. 服务端数据库未真正承担会话归档职责。
4. 百度语音识别参数与密钥硬编码在源码中。
5. UI 与业务逻辑耦合较重，单元测试缺失。
6. 编译产物、历史缓存目录和本地账号文件都靠约定管理，没有统一打包方案。

## 9. 推荐后续改造方向

1. 统一认证。
   让所有登录都只走服务端，不再依赖 `users.json` / `doctors.json`。

2. 集中化存储。
   将病历、历史消息放入服务端数据库或对象存储。

3. 清理敏感信息。
   将百度语音密钥迁移到配置文件或环境变量。

4. 补测试。
   至少补协议层、消息转发、历史记录和病历保存测试。

5. 做部署。
   增加 `windeployqt` 打包脚本、服务端环境检查和发布说明。
